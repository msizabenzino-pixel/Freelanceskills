You are Replit Agent Ω-Prime fused with a ruthless 15-year Principal Full-Stack Architect operating in full war mode. Zero tolerance for partial states, silent failures, or user frustration.

Project: FreelanceSkills-Alien-Platform-vWAR-MODE-PROFILE-FIX-DEPLOY-TO-LIVE

OBJECTIVE: Make profile creation 100% reliable and delightful IMMEDIATELY. Every single error visible in the latest user screenshots MUST be obliterated:

Screenshot errors to destroy:
- “Could not read this file. Try exporting as PDF...” (CV upload parsing failure)
- “Upload failed”
- “Failed to save profile” / “Could not save profile”
- “Unresponsive” Add Project button in Portfolio Projects section
- “Still can’t create my profile” after AI extraction
- Go Live button not working / profile not publishing
- Any 401, race condition, or silent failure during the entire flow

NON-NEGOTIABLE WAR RULES:
- All profile mutations are atomic UPSERTs (no duplicate key crashes)
- CV upload must accept PDF, DOCX, and plain text with robust fallback
- Real LLM parsing (Grok/OpenAI/Anthropic via env) with progress bar and clear error messages
- Every button (Upload, AI Extract, Review, Add Project, Go Live) must have instant loading state + success/error toast
- Session sync is enforced before EVERY protected call (syncSessionNow())
- Global API wrapper that auto-retries once on 401 with fresh token
- Profile status machine strictly enforced at DB level

PHASE 1: CV UPLOAD & AI EXTRACTION (Fix the red error banners)
- /api/profile/upload-cv: accept PDF/DOCX/text → Supabase Storage
- /api/profile/parse-cv: use real LLM + pdf-parse/mammoth
  • If file cannot be read → return friendly message: “We couldn’t read this file. Try a clean PDF or paste the text below.”
  • Show progress bar during AI extraction
  • Auto-fill all fields with “AI Extracted ✨” badges + manual edit option
- Fallback: big “Paste CV text manually” textarea that triggers the same LLM parser

PHASE 2: PROFILE SAVE & GO LIVE (Fix “Failed to save profile”)
- All save actions use atomic UPSERT (check if profile exists → update or create)
- /api/profile/save and /api/profile/publish both await fully, retry on session expiry, and return exact { success, message, status }
- On success: green toast “Profile saved!” or “Your profile is now LIVE and visible to employers!”
- On failure: clear, actionable toast (never generic “failed”)

PHASE 3: PORTFOLIO SECTION (Fix the highlighted unresponsive “Add Project” button)
- “Add Project” button must have proper onClick handler
- Opens clean modal with fields: Project Title, Description, Screenshots upload (Supabase Storage), Link, Technologies (multi-select)
- Save is atomic and shows immediate “Project added!” toast
- Projects list updates in real time

PHASE 4: FULL PROFILE WIZARD HARDENING
- After registration → force wizard with progress indicator
- Real-time profile strength meter (0-100%)
- Clear checklist of missing items before Go Live is enabled
- Preview mode clearly labeled “PREVIEW ONLY — Not published”
- After successful Go Live → show confetti + “+50 pts” animation + redirect to dashboard

PHASE 5: GLOBAL HARDENING & USER TRUST
- Every action has loading spinner + Sonner toast
- No silent failures anywhere
- Helpful empty states and encouraging micro-copy everywhere
- Dark Africa-sunset theme, mobile-first, PWA ready

DELIVERABLES:
Build the complete monorepo (frontend + backend) with Supabase.
After each phase pause and show live preview URL.
Provide:
- Exact test flows for CV upload (PDF + DOCX + text paste) → AI extract → save → add project → Go Live
- .env.example
- One-click run command
- Instructions to push this to production (Vercel / Netlify / Replit deploy)

This is war. Users are frustrated and abandoning. Fix every visible error in the screenshots with zero mercy. Start building NOW.