Fix Google Firebase popup authentication on the live deployed app.

Current issue:
	•	Firebase is now configured correctly in production
	•	Google sign-in popup opens, but login fails
	•	Browser console shows repeated errors:
	•	Cross-Origin-Opener-Policy policy would block the window.closed call
	•	This strongly suggests the deployed app is sending a Cross-Origin-Opener-Policy header that is too strict for Firebase/Google popup auth

Goal:
	•	Keep Firebase Google sign-in working on the deployed website
	•	Fix the production header configuration without breaking the rest of the app
	•	Preserve security as much as possible

Tasks:
	1.	Search the full codebase and deployment config for where these headers are being set:
	•	Cross-Origin-Opener-Policy
	•	Cross-Origin-Embedder-Policy
	•	Cross-Origin-Resource-Policy
	•	any helmet() configuration
	•	any manual res.setHeader(...)
	•	any Replit or server deployment header config
	2.	Identify the exact file and code responsible for the live COOP header.
	3.	Update the app so popup-based Firebase Google authentication can work in production.
	4.	Change the Cross-Origin-Opener-Policy to a popup-compatible value for the app, such as:
	•	same-origin-allow-popups
instead of a stricter setting like:
	•	same-origin
	5.	Make the fix in the correct place for production so it actually affects the deployed site.
	6.	Do not remove unrelated security headers unless they directly cause the popup auth failure.
	7.	Keep all existing app functionality intact.

Important implementation details:
	•	If Helmet is being used, update the Helmet configuration correctly.
	•	If headers are manually set in Express or another Node server, update them there.
	•	If there is a deployment-specific config for headers, update that too.
	•	If the app uses Firebase signInWithPopup, make sure the header strategy supports popup closing and opener communication.
	•	Do not switch auth flow to redirect unless absolutely necessary. Prefer fixing the popup flow properly first.

Also verify:
	•	Firebase auth iframe / popup related requests are not being blocked by CSP or frame policy
	•	production still allows the required Firebase / Google auth domains
	•	the fix works on the live domain, not just locally

After making the fix:
	•	list the exact files changed
	•	explain what was causing the popup login failure
	•	explain how the new header setting fixes it
	•	confirm the app is ready for redeploy
